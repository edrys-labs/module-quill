{
  "name": "module-quill",
  "version": "1.0.0",
  "scripts": {
    "build": "npx parcel build src/index.html --dist-dir docs --public-url ./",
    "watch": "npx parcel serve src/index.html --port 3000 --open",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "quill": "^2.0.2",
    "quill-cursors": "^4.0.3",
    "y-quill": "^1.0.0"
  },
  "devDependencies": {
    "buffer": "^6.0.3",
    "parcel": "^2.12.0",
    "process": "^0.11.10"
  }
}
